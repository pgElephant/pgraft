# PostgreSQL Configuration without pgraft (for testing)
listen_addresses = '*'
port = 5440
max_connections = 100
shared_buffers = 32MB
effective_cache_size = 1GB

# WAL settings
wal_level = replica
max_wal_senders = 10
max_replication_slots = 10
